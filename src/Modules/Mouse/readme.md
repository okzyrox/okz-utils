# Mouse

## Usage

### Mouse Hit Part
```lua

local Mouse = require(path.to.Mouse)

local HitPart: Instance? = Mouse.Hit()

-- Also accepts custom raycast parameters and distance; default distance is 1000.
-- local HitPart = Mouse.Hit(RaycastParams?, number?) 
```

See module for more available methods.